<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.example.finalapp">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.FinalApp">
        <activity android:name=".BookFavDetails"></activity>
        <activity android:name=".BookFavAdapter"
            tools:ignore="Instantiatable" />
        <activity android:name=".BookFavorites" />
        <activity android:name=".movieFavDetails" />
        <activity android:name=".MovieFavAdapter"
            tools:ignore="Instantiatable" />
        <activity android:name=".MovieFavorites" />
        <activity android:name=".BookDetails" />
        <activity android:name=".BookSearch" />
        <activity android:name=".MovieSearch" />
        <activity android:name=".ChangeProfile" />
        <activity android:name=".UserProfile" />
        <activity android:name=".Register" />
        <activity android:name=".SplashScreen">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".MainActivity" />

        <meta-data
            android:name="preloaded_fonts"
            android:resource="@array/preloaded_fonts" />
    </application>

</manifest>